 
 <style>
  .serchbtn{
    border-radius: 2rem;
    background-color: #fe424d;
    color:white;
  }
  .searchbar{
    width: 25rem;
    border-radius: 2rem;
    text-align: center;
  }
 </style>
 <nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <div class="ms-auto">
        <form class="d-flex " role="search" action="/signup">
        <input class="form-control me-2 searchbar" type="search" placeholder="Search destination" aria-label="Search"/>
        <button class="btn serchbtn btn-outline-danger" type="submit">Search</button>
      </form>
      </div>
      <div class="navbar-nav ms-auto">
       <div class="createlisting"> <a class="nav-link " href="/listings/new">Airbnb your home</a></div>
        <% if(!currentUser){%>          
          <a class="nav-link active" aria-current="page" href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Sign in</b></a>
          <% } %>
          <% if(currentUser) {%>
          <button class="btn btn-danger logout"><a href="/logout">Logout</a></button>
          <%}%>
      </div>
    </div>
     
  </div>
</nav>